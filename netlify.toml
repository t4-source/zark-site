[build]
  command = "npm ci && npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "18"
  NPM_VERSION = "9"
  NPM_FLAGS = "--version"

# Next.js static export redirects
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
